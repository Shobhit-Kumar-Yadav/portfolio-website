1. #!/bin/sh
read -p "enter the filename :" filename
if [ -d $filename ]
then 
echo "first is present"
else
echo "first is no present"
 
fi

3. Count the number of directories and files in a specific folder

# Replace /path/to/your/folder with your specific folder path
folder_path="/path/to/your/folder"

# Count directories
num_dirs=$(find "$folder_path" -type d | wc -l)

# Count files
num_files=$(find "$folder_path" -type f | wc -l)

echo "Number of directories: $num_dirs"
echo "Number of files: $num_files"

4. Find the smallest number from the array

# Define an array
array=(3 5 1 9 2)

# Find the smallest number
min=${array[0]}
for num in "${array[@]}"; do
  if (( num < min )); then
    min=$num
  fi
done

echo "The smallest number is: $min"
5. Find the sum of the array

# Define an array
array=(3 5 1 9 2)

# Calculate the sum
sum=0
for num in "${array[@]}"; do
  sum=$((sum + num))
done

echo "The sum of the array is: $sum"

6. Display all the directory names

# Replace /path/to/your/folder with your specific folder path
folder_path="/path/to/your/folder"

# Display all directory names
find "$folder_path" -type d

7. Check whether the number is a palindrome or not

# Function to check if a number is a palindrome
is_palindrome() {
  local num=$1
  local reversed_num=$(echo "$num" | rev)
  
  if [ "$num" -eq "$reversed_num" ]; then
    echo "$num is a palindrome"
  else
    echo "$num is not a palindrome"
  fi
}

# Replace 121 with your number
number=121
is_palindrome "$number"








--> Type of scalar function
    1.Numeric function     --> round,trunc,mod
    2.Character function   --> concat,lengthlower,upper,instr
    3. Date function            -->relate to date

Numeric function:-
````

select * from employee;

[ROUND]
------------
select round(100.2345,2) from dual; o/p: 100.23  ==>dual is a dummy table

select round(100.2345) from dual;    o/p:100

select round(100.7897) from dual;    o/p:101

select id,name,sal,sal/30,round(sal/30,2), round(sal/30) from employee;

[TRUNC]
-----------
select trunc(100.232476,3) from dual;  o/p:100.232
select trunc(100.9890) from dual;         o/p:100

[MOD]
---------
select  mod(10,2) from dual;
select mod(17,5) from dual;

Character function:-
```
select upper('mohit') from dual;

select id,name,upper(name), lower(name), initcap(name), length(name) from employee;

select * from employee where lenght(name)>5;

select * from employee where lenght(name)=6;

select id,name,concat('Welcome ',name), instr(name,'a')  from employee;

select id, name, dept, lpad(dept,12,'*') from employee;

select id, name, dept, rpad(dept,12,'*') from employee;

select id,name,replace(name,'a','j') from employee;

select instr('sachin','i') from dual; //finding location

Date function:-
`````
select sysdate from dual;

alter table employee add dob date;
alter table employee add doj date;
update emplloyee set dob=' ', doj=' ' where id=4; ,etc..................

select id,name,doj,doj+2,doj+20 from employee;



